interface:
  display_name: "Session Security"
  short_description: "Build secure, user-friendly session expiry"
  default_prompt: "Use $session-security to design or audit authenticated session timeout behavior."
